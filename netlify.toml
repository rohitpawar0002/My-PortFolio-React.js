[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# SPA fallback: Netlify runs serverless functions before matching this rule
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200